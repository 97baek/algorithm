const solution = (n, arr) => {
  let answer = [];
  let count = 0;
  let lp = 0;
  let rp = 0;

  while (count < 20) {
    if (rp < n) {
    }
    count++;
  }
  return answer.length;
};

const arr = [1, 3, 1, 2, 3];
console.log(solution(arr, 5));
